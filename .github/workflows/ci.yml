name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Create placeholders for embed
        run: |
          mkdir -p internal/server/dist && touch internal/server/dist/.gitkeep
          mkdir -p internal/core/ai/transcriber/bin
          touch internal/core/ai/transcriber/bin/whisper-darwin-arm64
          touch internal/core/ai/transcriber/bin/whisper-windows-amd64.exe
          touch internal/core/ai/transcriber/bin/whisper-linux-amd64
          touch internal/core/ai/transcriber/bin/sherpa-darwin-arm64
          touch internal/core/ai/transcriber/bin/sherpa-windows-amd64.exe
          touch internal/core/ai/transcriber/bin/sherpa-linux-amd64

      - name: Run tests
        run: CGO_ENABLED=0 go test -v ./...

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Create placeholders for embed
        run: |
          mkdir -p internal/server/dist && touch internal/server/dist/.gitkeep
          mkdir -p internal/core/ai/transcriber/bin
          touch internal/core/ai/transcriber/bin/whisper-darwin-arm64
          touch internal/core/ai/transcriber/bin/whisper-windows-amd64.exe
          touch internal/core/ai/transcriber/bin/whisper-linux-amd64
          touch internal/core/ai/transcriber/bin/sherpa-darwin-arm64
          touch internal/core/ai/transcriber/bin/sherpa-windows-amd64.exe
          touch internal/core/ai/transcriber/bin/sherpa-linux-amd64

      - name: Run staticcheck
        uses: dominikh/staticcheck-action@v1
        with:
          version: "latest"
        env:
          CGO_ENABLED: "0"
